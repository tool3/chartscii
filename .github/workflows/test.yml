name: test

on:
  push:
    branches:
    - debug/test

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - run: |
          echo $TERM
          yarn
          yarn test
